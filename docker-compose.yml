services:
  app:
    build: .
    ports:
      - "8000:8000"

    environment:
      AWS_REGION: us-east-1
      CHROMA_PERSIST_DIR: /home/appuser/chroma

    volumes:
      - ./.chroma:/home/appuser/chroma
      - ~/.aws:/home/appuser/.aws:ro

volumes:
  chroma_data: